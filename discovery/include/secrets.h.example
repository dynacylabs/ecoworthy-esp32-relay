// Copy to secrets.h and fill in real values. secrets.h is gitignored.
#pragma once

#define WIFI_SSID "changeme"
#define WIFI_PASSWORD "changeme"

// Optional static IP. Leave WIFI_STATIC_IP undefined/blank to use DHCP instead.
#define WIFI_STATIC_IP "192.168.2.4"
#define WIFI_GATEWAY "192.168.2.1"
#define WIFI_SUBNET "255.255.255.0"
#define WIFI_DNS "192.168.2.1"

// Used for ArduinoOTA (Sketch -> Upload Using: OTA) and to protect the OTA port.
#define OTA_PASSWORD "changeme"

// Shown in Serial/mDNS and used as the ArduinoOTA hostname.
#define DEVICE_HOSTNAME "hobocamp-bw0f-logger"
