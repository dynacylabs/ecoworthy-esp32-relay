fastapi==0.115.0
uvicorn[standard]==0.30.6
asyncpg==0.29.0
pydantic==2.9.2
aioesphomeapi==45.7.0
httpx==0.27.2
PyYAML==6.0.3
# Used by the Firmware tab (app/firmware.py) to compile/upload the ESP32
# firmware in-process instead of embedding the official ESPHome
# dashboard/device-builder container - see README's "Firmware updates".
# Pinned to match the esphome/esphome:latest image version we tested
# against.
esphome==2026.7.2
